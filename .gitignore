# Environment and secrets (never commit)
.env
.env.local
.env.*.local
.env.bak*
*.pem

# Python
venv/
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.mypy_cache/
.Python
*.so
.eggs/
dist/
build/
# ...but NOT the Next.js /build route (this bare `build/` is for Python build artifacts)
!/apps/analytics-portal/src/app/build/
*.egg-info/
*.egg

# IDE and OS
.idea/
.vscode/
.cursor/mcp.json
*.swp
.DS_Store
Thumbs.db

# Project outputs (optional; remove if you want to track narrative.json)
output/narrative.json

# Generated Jasper artifacts
*.jasper
deploy/*_report_unit/
deploy/*_report_unit.zip
deploy/jaspersoft_client_promotion/archive/*.zip
deploy/jaspersoft_client_promotion/incoming_datasources/*.zip
deploy/jaspersoft_client_promotion/incoming_exports/*.zip
deploy/jaspersoft_client_promotion/prepared_imports/*.zip
deploy/jaspersoft_client_promotion/prepared_imports/_tmp/
deploy/jaspersoft_standard_offering/Standard_Offering_import/

# Coverage / logs
coverage/
htmlcov/
*.log
tmp/
node_modules/
apps/analytics-portal/.next/

# Oracle / connection strings and API keys are in .env (see above)
output/.portal_data_source.vault
output/.portal_vault_key

# Domain working area



# Validation outputs

# Portal identity store: password hashes and per-tenant user rows.
# Local dev artefact, recreated on first run; must never be a repo file.
data/analytics_portal/*.db
