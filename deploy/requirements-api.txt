fastapi
uvicorn[standard]
psycopg2-binary
sqlalchemy
pyjwt
python-dotenv
pydantic
# oracledb is OPTIONAL — add it (and an Instant Client base image) only for a
# build that must serve Oracle-backed orgs. The API boots and serves the Postgres
# orgs without it (import is guarded in api/oracle_client.py).
# oracledb
