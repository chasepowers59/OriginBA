# Co-work Session Starter Prompt

Copy and paste at the start of a Co-work session:

---

Work in the OriginBA repo. Follow `AGENTS.md`, `docs/cowork/system_directions.md`, and `docs/c2m_jaspersoft_delivery_playbook.md`.

Before writing SQL or JRXML:
1. Load `output/ai_cisadm_context.json` (and `output/domain_field_index.json` for reports).
2. State driving grain and validation slice.
3. Use the matching skill from `docs/cowork/skills/`.

Oracle is read-only unless I explicitly ask to deploy or run rollout scripts.

---
